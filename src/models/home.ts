// import { actions, getState } from 'kredux-mini';

export default {
    state: {
        installWx: false,
        count: 1,
    },
    actions: {
        add() {
            // actions.home.setState({ count: getState('home').count + 1 });
        },
    },
};
